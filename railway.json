{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cp .env.example .env && php artisan key:generate --force && npm ci && npm run build"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
